import s from "../../Dashboard-style/Dashboard.module.scss";


const Baner = () => {
  return (
    <section className={s.headerContainer__projectTitle}>
      <h2>
        <span className={s.alice}>Alice</span> Virtual AI Therapist
      </h2>
    </section>
  );
};

export default Baner;
